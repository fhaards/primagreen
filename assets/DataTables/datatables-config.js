$(document).ready(function () {
	var table = $("#primaTable").DataTable({
        responsive: true,
        pageLength : 10,
	});
});

$(document).ready(function () {
	var table = $("#primaTable2").DataTable({
		responsive: true,
		pageLength : 10,
	});
});